# 阶段二：缺失后台模块补齐任务清单

[返回总栏](./后台管理完善计划.md) | [对应任务文档](./阶段二-缺失后台模块补齐.md) | [上一阶段](./阶段一-后台基础闭环校准-任务清单.md) | [下一阶段](./阶段三-已有后台模块一致性治理-任务清单.md)

## 1. 阶段摘要

- 当前状态：`doing`
- 前置依赖：阶段一基线结论
- 完成判定：`follow / file / chat` 均完成正式收口与验收

## 2. Follow

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 落地关注关系列表页与基础筛选能力 | `Follows.vue` 首版页面 | 页面可查询关系数据 |
| `done` | 接入 `follow` Store 和后台 Mock | `follow` Store / Mock | 页面可在默认 Mock 环境下运行 |
| `doing` | 校准菜单、权限、按钮链路 | 菜单与权限收口结果 | `content:follow:*` 行为与页面一致 |
| `todo` | 完成异常清理流程验收与提示语统一 | Follow 模块验收记录 | 清理结果、提示语和权限反馈正确 |

## 3. File

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 落地文件库列表、详情抽屉、上传任务页 | `Files.vue` 与详情组件首版 | 文件与任务数据可查看 |
| `done` | 接入 `file` Store 和后台 Mock | `file` Store / Mock | 默认 Mock 环境下可联调 |
| `doing` | 校准文件状态切换、删除和引用展示闭环 | File 模块治理结果 | 状态、删除、详情联动正确 |
| `todo` | 完成上传任务失败态、筛选项和反馈语统一验收 | File 模块验收记录 | 失败任务、筛选和提示语满足标准 |

## 4. Chat

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 落地会话治理页、消息详情抽屉与成员管理入口 | `Chats.vue` 与详情组件首版 | 会话、成员、消息可查看 |
| `done` | 接入 `chat` Store 和后台 Mock | `chat` Store / Mock | 默认 Mock 环境下可联调 |
| `doing` | 校准成员角色、成员状态、禁言、撤回、会话状态更新链路 | Chat 治理动作收口结果 | 关键治理动作有正确反馈 |
| `todo` | 完成消息回执、消息详情和会话详情联动验收 | Chat 模块验收记录 | 回执、详情、消息链路一致 |

## 5. 阶段级收口

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `todo` | 汇总三个模块的正式验收结果并回写总栏 | 阶段总结 | 总栏中的阶段状态可更新 |
| `todo` | 确认三个模块均达到可进入阶段三/五的输入标准 | 阶段完成判定 | 后续阶段无需补缺失模块基础链路 |

## 6. 当前阻塞

- 暂无正式标记的 `blocked` 项

## 7. 已完成记录

- `follow / file / chat` 的首版页面、Store 和 Mock 已存在
