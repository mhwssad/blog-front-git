# 阶段二：缺失后台模块补齐任务清单

[返回总栏](./后台管理完善计划.md) | [对应任务文档](./阶段二-缺失后台模块补齐.md) | [上一阶段](./阶段一-后台基础闭环校准-任务清单.md) | [下一阶段](./阶段三-已有后台模块一致性治理-任务清单.md)

## 1. 阶段摘要

- 当前状态：`done`
- 前置依赖：阶段一基线结论
- 完成判定：`follow / file / chat` 均完成正式收口与验收

## 2. Follow

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 落地关注关系列表页与基础筛选能力 | `Follows.vue` 首版页面 | 页面可查询关系数据 |
| `done` | 接入 `follow` Store 和后台 Mock | `follow` Store / Mock | 页面可在默认 Mock 环境下运行 |
| `done` | 校准菜单、权限、按钮链路 | 菜单与权限收口结果 | `content:follow:*` 行为与页面一致 |
| `done` | 补齐 `createdAt` 列、完成异常清理流程验收与提示语统一 | Follow 模块验收记录 | 清理结果、提示语和权限反馈正确 |

## 3. File

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 落地文件库列表、详情抽屉、上传任务页 | `Files.vue` 与详情组件首版 | 文件与任务数据可查看 |
| `done` | 接入 `file` Store 和后台 Mock | `file` Store / Mock | 默认 Mock 环境下可联调 |
| `done` | 校准文件状态切换、删除和引用展示闭环 | File 模块治理结果 | 状态、删除、详情联动正确 |
| `done` | 补齐详情抽屉 references（category、createdAt）和 tasks（originalName、fileSize、isQuickUpload、isChunked）列 | File 模块验收记录 | 失败任务、筛选和提示语满足标准 |

## 4. Chat

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 落地会话治理页、消息详情抽屉与成员管理入口 | `Chats.vue` 与详情组件首版 | 会话、成员、消息可查看 |
| `done` | 接入 `chat` Store 和后台 Mock | `chat` Store / Mock | 默认 Mock 环境下可联调 |
| `done` | 校准成员角色、成员状态、禁言、撤回、会话状态更新链路 | Chat 治理动作收口结果 | 关键治理动作有正确反馈 |
| `done` | 补充会话表（isAllSite、createdAt）、消息表（edited、createdAt）、修复撤回权限（content:chat:revoke）、成员操作添加 v-permission 守卫 | Chat 模块验收记录 | 回执、详情、消息链路一致 |

## 5. 阶段级收口

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 汇总三个模块的正式验收结果并回写总栏 | 阶段总结 | 总栏中的阶段状态可更新 |
| `done` | 确认三个模块均达到可进入阶段三/五的输入标准 | 阶段完成判定 | 后续阶段无需补缺失模块基础链路 |

## 6. 已完成记录

- `follow / file / chat` 的首版页面、Store 和 Mock 已存在
- Follow：补充 createdAt 列，权限链路校准完成
- File：详情抽屉补充 references（category、createdAt）和 tasks（originalName、fileSize、isQuickUpload、isChunked）列
- Chat：修复撤回权限为 `content:chat:revoke`，补充会话表 isAllSite/createdAt 列，补充消息表 edited/createdAt 列，成员管理操作添加 v-permission 守卫
- 三个模块 type-check 和 build 验证通过
