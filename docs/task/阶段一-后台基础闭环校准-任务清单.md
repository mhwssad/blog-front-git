# 阶段一：后台基础闭环校准任务清单

[返回总栏](./后台管理完善计划.md) | [对应任务文档](./阶段一-后台基础闭环校准.md) | [下一阶段](./阶段二-缺失后台模块补齐-任务清单.md)

## 1. 阶段摘要

- 当前状态：`done`
- 前置依赖：无
- 完成判定：后台基线、模块状态和计划一致性被正式确认

## 2. 任务清单

| 状态 | 任务 | 交付物 | 验收方式 |
| --- | --- | --- | --- |
| `done` | 盘点 `src/views/admin/**`、`src/api/sys/**`、`src/stores/modules/**`、`mock/**` 的现有后台链路 | 基线盘点结果 | 现有后台模块可被列表化说明 |
| `done` | 核对固定首页、动态菜单、路由守卫、标签页和按钮权限链路 | 基础链路校准结论 | 可明确指出链路是否完整及缺口位置 |
| `done` | 明确 `footprint` 模块纳入后台范围并补齐页面、Store、菜单权限和 Mock 删除链路 | `footprint` 状态结论 | 阶段三清单不再对 `footprint` 状态存在歧义 |
| `done` | 回写总栏中的“当前仓库基线”和阶段状态 | 更新后的总栏文档 | 总栏与仓库现状一致 |
| `done` | 输出阶段二至阶段五的准确输入条件，并消除 `footprint` 范围歧义 | 各阶段输入说明 | 后续阶段文档无需再次重复盘点 |

## 3. 当前阻塞

- 暂无正式标记的 `blocked` 项

## 4. 已完成记录

- 已确认 `follow / file / chat` 首版页面、Store 和 Mock 已存在
- 已确认后台首页已有总览首版
- 已确认 `/admin/dashboard`、动态菜单注册、守卫重匹配、标签页和按钮权限链路可用
- 已补齐 `footprint` 后台页面、Store、菜单权限和 Mock 删除/条件清空链路
