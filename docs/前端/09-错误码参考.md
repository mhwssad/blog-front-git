# 错误码参考

> 后端所有错误码及前端处理策略。HTTP 响应体统一为 `Result<T>` 格式，`code` 为业务错误码，非 HTTP 状态码。

## HTTP 状态码与业务错误码映射

| HTTP Status | 业务 code 范围 | 含义 |
|---|---|---|
| 200 | 200 | 成功 |
| 400 | 40001-40011 | 参数校验/请求错误 |
| 401 | 40100-40115 | 认证错误 |
| 403 | 40300-40303 | 权限错误 |
| 404 | 40400-40401 | 资源不存在 |
| 429 | 42900 | 频率限制 |
| 500 | 500, 50000-50020 | 系统错误 |

## 通用错误码 (ResultErrorCode)

### 成功

| code | 说明 |
|---|---|
| 200 | 成功 |

### 参数校验错误 (40001-40011)

| code | 说明 | 前端处理 |
|---|---|---|
| 40001 | 参数校验失败 | 显示字段级错误提示 |
| 40002 | 请求体解析失败 | 检查请求格式 |
| 40003 | 缺少必需参数 | 补全参数 |
| 40004 | 参数类型不匹配 | 检查参数类型 |
| 40005 | 参数范围越界 | 调整参数值 |
| 40006 | 请求方法不支持 | 检查 HTTP 方法 |
| 40007 | 重复提交 | 提示用户稍后重试 |
| 40008 | 文件上传失败 | 检查文件格式和大小 |
| 40009 | 文件大小超限 | 提示文件过大 |
| 40010 | 文件类型不允许 | 提示支持的文件类型 |
| 40011 | 请求频率超限 | 提示稍后重试 |

### 认证错误 (40100-40115)

| code | 说明 | 前端处理 |
|---|---|---|
| 40100 | 未登录 | 跳转登录页 |
| 40101 | Token 已过期 | 尝试刷新 Token |
| 40102 | Token 无效 | 跳转登录页 |
| 40103 | Token 已被吊销 | 跳转登录页 |
| 40104 | 账号已被禁用 | 提示联系管理员 |
| 40105 | 账号已被删除 | 提示账号不存在 |
| 40106 | 密码错误 | 提示重新输入 |
| 40107 | 账号不存在 | 提示注册 |
| 40108 | 邮箱验证码错误 | 重新输入 |
| 40109 | 邮箱验证码已过期 | 重新发送 |
| 40110 | 邮箱验证码发送过于频繁 | 提示稍后重试 |
| 40111 | 邮箱未注册 | 提示注册 |
| 40112 | 邮箱已注册 | 提示直接登录 |
| 40113 | 用户名已存在 | 提示换用户名 |
| 40114 | 刷新 Token 已过期 | 跳转登录页 |
| 40115 | 刷新 Token 无效 | 跳转登录页 |

### 权限错误 (40300-40303)

| code | 说明 | 前端处理 |
|---|---|---|
| 40300 | 无访问权限 | 显示 403 页面 |
| 40301 | CSRF Token 无效 | 刷新页面 |
| 40302 | IP 被限制 | 提示联系管理员 |
| 40303 | 接口访问频率超限 | 提示稍后重试 |

### 资源错误 (40400-40401)

| code | 说明 | 前端处理 |
|---|---|---|
| 40400 | 资源不存在 | 显示 404 页面 |
| 40401 | 接口不存在 | 检查请求路径 |

### 系统错误 (500, 50000-50020)

| code | 说明 | 前端处理 |
|---|---|---|
| 500 | 操作失败 | 显示错误信息 |
| 50000 | 系统内部异常 | 提示稍后重试 |
| 50001 | 数据库异常 | 提示稍后重试 |
| 50010 | 服务不可用 | 提示稍后重试 |

### 频率限制

| code | 说明 | 前端处理 |
|---|---|---|
| 42900 | 请求频率超限 | 提示稍后重试 |

## 文件模块错误码 (FileResultCode)

| code | 说明 | 前端处理 |
|---|---|---|
| 71001 | 存储节点未配置 | 联系管理员 |
| 71002 | 文件不能为空 | 选择文件 |
| 71003 | 分片序号越界 | 检查分片参数 |
| 71004 | 上传任务不存在 | 重新初始化 |
| 71005 | 上传任务已过期 | 重新初始化 |
| 71006 | MD5 校验失败 | 重新上传 |
| 71007 | 分片已上传 | 跳过该分片 |
| 71008 | 文件大小不匹配 | 重新上传 |
| 71009 | 不支持的文件类型 | 检查文件格式 |
| 71010 | 文件不存在 | 检查文件引用 |
| 71011 | 不允许的操作 | 检查操作合法性 |
| 71012 | 分片数量不匹配 | 重新上传 |
| 71013 | 秒传文件不存在 | 走普通上传 |
| 71014 | 业务引用已存在 | 检查是否重复引用 |
| 71015 | 文件正在被引用，无法删除 | 先解除引用 |
| 71016 | 存储类型不支持 | 联系管理员 |
| 71017 | 分片上传未完成 | 完成所有分片上传 |
| 71018 | 文件名包含非法字符 | 修改文件名 |
| 71019 | 文件路径无效 | 重新上传 |
| 71020 | 存储空间不足 | 联系管理员 |
| 71021 | 文件大小超过限制 | 压缩后重试 |
| 71022 | 分片大小不一致 | 重新上传 |
| 71023 | 上传任务状态不允许此操作 | 检查任务状态 |
| 71024 | 文件已被删除 | 重新上传 |
| 71025 | 不允许删除已删除的文件 | 无需操作 |
| 71026 | 秒传仅支持已完成的文件 | 走普通上传 |

## 存储模块错误码 (StorageResultCode)

| code | 说明 | 前端处理 |
|---|---|---|
| 70001 | 文件上传失败 | 重新上传 |
| 70002 | 获取文件流失败 | 联系管理员 |
| 70003 | 文件删除失败 | 联系管理员 |
| 70004 | 文件不存在 | 检查文件引用 |
| 70005 | 存储服务不可用 | 稍后重试 |
| 70006 | 存储配置错误 | 联系管理员 |
| 70007 | 存储节点不可用 | 稍后重试 |

## 前端全局错误处理策略

### 错误类型与处理策略矩阵

| 错误类型 | code 范围 | 处理策略 |
|---|---|---|
| 成功 | 200 | 正常处理 data |
| 参数错误 | 40001-40011 | 表单字段级提示 |
| Token 过期 | 40101 | 自动刷新 → 重试原请求 |
| 认证失败 | 40100, 40102-40115 | 清除 token → 跳转登录页 |
| 权限不足 | 40300-40303 | 显示 403 页面或提示 |
| 资源不存在 | 40400-40401 | 显示 404 页面 |
| 频率限制 | 42900, 40011 | Toast 提示稍后重试 |
| 文件错误 | 71001-71026 | Toast 提示具体错误 |
| 存储错误 | 70001-70007 | Toast 提示稍后重试 |
| 系统错误 | 500, 50000-50020 | Toast 提示系统繁忙 |

### Axios 拦截器完整实现

```javascript
import axios from 'axios'

const request = axios.create({
  baseURL: 'http://localhost:8000',
  timeout: 30000,
})

let isRefreshing = false
let pendingRequests = []

// 请求拦截器 — 注入 Token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('accessToken')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器 — 统一错误处理
request.interceptors.response.use(
  response => {
    const { code, message, data } = response.data

    if (code === 200) {
      return data
    }

    // Token 过期 → 尝试刷新
    if (code === 40101) {
      return handleTokenRefresh(response.config)
    }

    // 其他认证错误 → 跳转登录
    if (code >= 40100 && code <= 40115) {
      clearAuthAndRedirect()
      return Promise.reject(new Error(message))
    }

    // 权限错误
    if (code >= 40300 && code <= 40303) {
      router.push('/403')
      return Promise.reject(new Error(message))
    }

    // 资源不存在
    if (code === 40400) {
      router.push('/404')
      return Promise.reject(new Error(message))
    }

    // 频率限制
    if (code === 42900 || code === 40011) {
      showToast('操作过于频繁，请稍后重试')
      return Promise.reject(new Error(message))
    }

    // 文件/存储模块错误
    if ((code >= 71001 && code <= 71026) || (code >= 70001 && code <= 70007)) {
      showToast(message || '文件操作失败')
      return Promise.reject(new Error(message))
    }

    // 参数校验错误
    if (code >= 40001 && code <= 40011) {
      showToast(message || '参数错误')
      return Promise.reject(new Error(message))
    }

    // 其他错误
    showToast(message || '操作失败')
    return Promise.reject(new Error(message))
  },
  error => {
    // HTTP 网络错误
    showToast('网络异常，请检查网络连接')
    return Promise.reject(error)
  }
)

// Token 刷新逻辑（防止并发刷新）
async function handleTokenRefresh(failedConfig) {
  if (!isRefreshing) {
    isRefreshing = true
    try {
      const refreshToken = localStorage.getItem('refreshToken')
      const { data } = await axios.post('/api/auth/refresh', { refreshToken })
      localStorage.setItem('accessToken', data.accessToken)
      localStorage.setItem('refreshToken', data.refreshToken)
      // 重试所有排队的请求
      pendingRequests.forEach(cb => cb(data.accessToken))
      pendingRequests = []
    } catch {
      clearAuthAndRedirect()
      return Promise.reject(new Error('刷新 Token 失败'))
    } finally {
      isRefreshing = false
    }
  }

  // 将当前请求加入等待队列
  return new Promise(resolve => {
    pendingRequests.push(token => {
      failedConfig.headers.Authorization = `Bearer ${token}`
      resolve(request(failedConfig))
    })
  })
}

function clearAuthAndRedirect() {
  localStorage.removeItem('accessToken')
  localStorage.removeItem('refreshToken')
  router.push('/login')
}

function showToast(msg) {
  // 使用项目 UI 框架的 Toast 组件
  console.warn('[Error]', msg)
}
```
