# 联调环境 — 关闭 Mock，直连后端
# 启动: pnpm dev:staging
# 构建: pnpm build:staging

# API
VITE_API_BASE_URL=http://localhost:8080/api
VITE_ENABLE_MOCK=false
VITE_DEV_PROXY_TARGET=localhost:8080

# 日志
VITE_LOG_LEVEL=info
VITE_LOG_BUFFER_SIZE=150
VITE_ENABLE_LOG_PERSISTENCE=false
